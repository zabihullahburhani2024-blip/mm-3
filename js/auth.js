/** @deprecated Use register.js + activation.js — kept for compatibility */
(function () {
  if (!window.MM_AUTH && window.console) {
    console.warn('Load js/activation.js and js/register.js before app scripts.');
  }
})();
